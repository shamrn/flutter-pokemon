// The file contains common settings for the project

import 'package:flutter/material.dart';

class AppSettings {
  static const Duration splashScreenDuration = Duration(seconds: 2);
  static const defaultLocale = Locale('ru');
}
